{% extends 'base.html.twig' %}

{% block title %}BitChest - EditPassword
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			max-width: 40rem;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper">
		<h1 class="mt-4">Password Edition -
			{{user.fullName}}
			✅</h1>
		{% for message in app.flashes('warning') %}
			<div class="alert alert-warning mt-4">
				{{ message }}
			</div>
		{% endfor %}
		{{ form(form)}}
		</div>
	{% endblock %}
